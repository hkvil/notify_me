import 'package:flutter/material.dart';

class SubsribedScreen extends StatelessWidget {
  const SubsribedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Subsribed Page!'),
    );
  }
}
