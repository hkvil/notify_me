import 'package:flutter/material.dart';

class SearchBarComic extends StatelessWidget {
  const SearchBarComic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40.0,
      color: Colors.grey,
      child: const Text('Search Bar'),
    );
  }
}
